/// This is the main function; it is the entry point for the compiled program.  Here, it returns the exit
/// code of the application.  0 means that the application exited successfully and no errors occurred, and
/// any non-0 exit codes implies that the application exited unsuccessfully and/or an error occurred.
@func main(): usize {
    // You don't need to use a `return` statement to return a value, if you use the value that you want
    // to return without a `;` (semicolon) after it, the compiler assumes you were trying to return it.
    //
    // This is the exit code of the application.
    0
}